// Criando o frontend com React

// 1. Criar o projeto React
```sh
npx create-react-app painel-iptv-frontend
cd painel-iptv-frontend
npm install
```

// 2. Instalar dependências necessárias
```sh
npm install react-router-dom axios @mui/material @mui/icons-material @emotion/react @emotion/styled react-toastify chart.js react-chartjs-2
```

// 3. Estrutura de pastas
```
/painel-iptv-frontend
│── /src
│   │── /components
│   │   │── Sidebar.js
│   │   │── Header.js
│   │   │── ProtectedRoute.js
│   │   │── DashboardChart.js
│   │── /pages
│   │   │── Dashboard.js
│   │   │── Login.js
│   │   │── Usuarios.js
│   │   │── Servidores.js
│   │   │── Clientes.js
│   │   │── Assinaturas.js
│   │   │── Transacoes.js
│   │── /services
│   │   │── api.js
│   │── App.js
│   │── index.js
```

// 4. Criar `Sidebar.js` para navegação
```js
import React from 'react';
import { Link } from 'react-router-dom';
import { Drawer, List, ListItem, ListItemText } from '@mui/material';

const Sidebar = () => {
    return (
        <Drawer variant="permanent" anchor="left">
            <List>
                <ListItem button component={Link} to="/dashboard">
                    <ListItemText primary="Dashboard" />
                </ListItem>
                <ListItem button component={Link} to="/usuarios">
                    <ListItemText primary="Usuários" />
                </ListItem>
                <ListItem button component={Link} to="/servidores">
                    <ListItemText primary="Servidores" />
                </ListItem>
                <ListItem button component={Link} to="/clientes">
                    <ListItemText primary="Clientes" />
                </ListItem>
                <ListItem button component={Link} to="/assinaturas">
                    <ListItemText primary="Assinaturas" />
                </ListItem>
                <ListItem button component={Link} to="/transacoes">
                    <ListItemText primary="Transações" />
                </ListItem>
            </List>
        </Drawer>
    );
};

export default Sidebar;
```

// 5. Atualizar `Dashboard.js` para incluir Sidebar
```js
import React, { useEffect, useState } from 'react';
import api from '../services/api';
import DashboardChart from '../components/DashboardChart';
import Sidebar from '../components/Sidebar';

const Dashboard = () => {
    const [chartData, setChartData] = useState({ labels: [], values: [] });

    useEffect(() => {
        async function fetchData() {
            try {
                const response = await api.get('/assinaturas');
                const labels = response.data.map(item => item.nome);
                const values = response.data.map(item => item.quantidade);
                setChartData({ labels, values });
            } catch (error) {
                console.error('Erro ao buscar dados', error);
            }
        }
        fetchData();
    }, []);

    return (
        <div style={{ display: 'flex' }}>
            <Sidebar />
            <div style={{ marginLeft: 240, padding: 20, width: '100%' }}>
                <h1>Bem-vindo ao Painel IPTV</h1>
                <DashboardChart data={chartData} />
            </div>
        </div>
    );
};

export default Dashboard;
```

Agora o painel tem uma **Sidebar fixa** para navegação e melhor organização! 🚀🎨
